import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    travelytext:{
        alignContent : 'center',
        marginTop: '100px',
        fontWeight: 'bold',
    }
});

export default styles;